fastapi>=0.110.0
uvicorn>=0.27.0
pydantic>=2.0.0
cryptography>=42.0.0
markdown>=3.5

# Optional: transactional email for magic links (Phase B.9).
# If unset, magic links log to stdout (dev/test only).
resend>=0.8.0

# Optional: self-hosted config parsing + canonical text extraction (Piece 14).
# Install ONLY in self-hosted deployments that attest files.
PyYAML>=6.0
pdfplumber>=0.11.0
python-docx>=1.1.0
openpyxl>=3.1.0
python-pptx>=0.6.23
imagehash>=4.3.1
pikepdf>=8.0.0
psutil>=5.9.0

# Optional: server-side PDF rendering for /one-pager.pdf and similar
# routes. On Linux, also requires:
#   apt install libpango-1.0-0 libpangoft2-1.0-0
# If WeasyPrint is not installed, the .pdf routes return 503 with a
# helpful message; the HTML versions of those pages still work.
weasyprint>=60.0

# Dev API tier only (see litellm-plugin/):
# litellm[proxy]>=1.80.0
